<Window x:Class="ClientCentralino_vs2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ClientCentralino_vs2"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        mc:Ignorable="d"
        Title="Centralino Manager" Height="850" Width="1200"
        FontFamily="Segoe UI" FontSize="13"
        WindowStartupLocation="CenterScreen"
        Background="#FFF5F5F5">

    <Window.Resources>
        <!-- Stili comuni -->
        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="Foreground" Value="#FF333333"/>
        </Style>

        <Style x:Key="LightModernButton" TargetType="Button">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#FF333333"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FFE6F2FA"/>
                                <Setter Property="BorderBrush" Value="#FF007ACC"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#FF007ACC"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LightModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#FF333333"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="LightModernTabControl" TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <Style x:Key="LightModernTabItem" TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="BorderThickness" Value="1,1,1,0"/>
            <Setter Property="Padding" Value="15,5"/>
            <Setter Property="Foreground" Value="#FF666666"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid>
                            <Border Name="Border" Background="#FFF5F5F5" BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4,4,0,0">
                                <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center"
                                                  HorizontalAlignment="Center" ContentSource="Header"
                                                  Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" TargetName="Border" Value="White"/>
                                <Setter Property="Foreground" Value="#FF007ACC"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" TargetName="Border" Value="#FFEEEEEE"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LightModernGroupBox" TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#FF666666"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Border BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4" Background="{TemplateBinding Background}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <Border Grid.Row="0" Background="#FFF5F5F5" CornerRadius="3,3,0,0"
                                        Padding="5" Margin="-1,-1,-1,0">
                                    <ContentPresenter ContentSource="Header" 
                                                      RecognizesAccessKey="True"
                                                      HorizontalAlignment="Left"
                                                      VerticalAlignment="Center"/>
                                </Border>

                                <ContentPresenter Grid.Row="1" Margin="{TemplateBinding Padding}"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LightModernDataGrid" TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#FF333333"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#FFF5F5F5"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserDeleteRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.PanningMode" Value="Both"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#FFF5F5F5"/>
                        <Setter Property="Foreground" Value="#FF666666"/>
                        <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderBrush" Value="#FFEEEEEE"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border BorderBrush="{TemplateBinding BorderBrush}" 
                                            BorderThickness="{TemplateBinding BorderThickness}" 
                                            Background="{TemplateBinding Background}" 
                                            SnapsToDevicePixels="True">
                                        <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#FF007ACC"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ChartBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#FFCCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,10,0"/>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Header -->
        <Border Grid.Row="0" Background="#FF007ACC" Height="40" VerticalAlignment="Top">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="15,0">
                <Path Data="M19,3H5C3.89,3 3,3.89 3,5V19A2,2 0 0,0 5,21H19A2,2 0 0,0 21,19V5C21,3.89 20.1,3 19,3Z" 
                      Fill="White" Stretch="Uniform" Width="20" Height="20" Margin="0,0,10,0"/>
                <TextBlock Text="CENTRALINO MANAGER" FontSize="16" FontWeight="Bold" Foreground="White"/>
            </StackPanel>
        </Border>

        <!-- Main Content -->
        <TabControl x:Name="TabControl" Grid.Row="1" Margin="10,50,10,10" 
                    Style="{StaticResource LightModernTabControl}">

            <!-- Chiamate Tab -->
            <TabItem Header="CHIAMATE" Style="{StaticResource LightModernTabItem}">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Filtri -->
                    <GroupBox Header="Filtri" Grid.Row="0" Style="{StaticResource LightModernGroupBox}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Filtra per numero:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox x:Name="TxtFilterNumber" Width="250" Margin="0,0,10,0" Style="{StaticResource LightModernTextBox}"/>
                            <Button x:Name="BtnFilterCalls" Content="Filtra" Width="100" Style="{StaticResource LightModernButton}" Click="BtnFilterCalls_Click"/>
                            <Button x:Name="BtnRefreshCalls" Content="Aggiorna" Width="100" Margin="10,0,0,0" Style="{StaticResource LightModernButton}" Click="BtnRefreshCalls_Click"/>
                        </StackPanel>
                    </GroupBox>

                    <!-- Griglia chiamate -->
                    <DataGrid x:Name="DgCalls" Grid.Row="1" Style="{StaticResource LightModernDataGrid}"
                              SelectionChanged="DgCalls_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Tipo" Binding="{Binding TipoChiamata}" Width="100"/>
                            <DataGridTextColumn Header="Numero Chiamante" Binding="{Binding NumeroChiamante}" Width="150"/>
                            <DataGridTextColumn Header="Numero Chiamato" Binding="{Binding NumeroChiamato}" Width="150"/>
                            <DataGridTextColumn Header="Data Arrivo" Binding="{Binding DataArrivoChiamata, StringFormat=dd/MM/yyyy HH:mm:ss}" Width="180"/>
                            <DataGridTextColumn Header="Data Fine" Binding="{Binding DataFineChiamata, StringFormat=dd/MM/yyyy HH:mm:ss}" Width="180"/>
                            <DataGridTextColumn Header="Località" Binding="{Binding Locazione}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Aggiorna località -->
                    <GroupBox Header="Aggiorna Località" Grid.Row="2" Style="{StaticResource LightModernGroupBox}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Località:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox x:Name="TxtLocation" Width="400" Margin="0,0,10,0" Style="{StaticResource LightModernTextBox}"/>
                            <Button x:Name="BtnUpdateLocation" Content="Aggiorna" Width="120" Style="{StaticResource LightModernButton}" Click="BtnUpdateLocation_Click"/>
                        </StackPanel>
                    </GroupBox>
                </Grid>
            </TabItem>

            <!-- Contatti Tab -->
            <TabItem Header="CONTATTI" Style="{StaticResource LightModernTabItem}">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Cerca contatto -->
                    <GroupBox Header="Cerca Contatto" Grid.Row="0" Style="{StaticResource LightModernGroupBox}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="Numero:" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox x:Name="TxtSearchContact" Grid.Column="1" Margin="0,0,10,0" Style="{StaticResource LightModernTextBox}"/>
                            <Button x:Name="BtnSearchContact" Grid.Column="2" Content="Cerca" Width="100" Style="{StaticResource LightModernButton}" Click="BtnSearchContact_Click"/>
                        </Grid>
                    </GroupBox>

                    <!-- Dettagli contatto -->
                    <GroupBox Header="Dettagli Contatto" Grid.Row="1" Style="{StaticResource LightModernGroupBox}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Numero:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,10,5"/>
                            <TextBox x:Name="TxtContactNumber" Grid.Row="0" Grid.Column="1" Margin="0,5,10,5" Style="{StaticResource LightModernTextBox}"/>

                            <TextBlock Text="Ragione Sociale:" Grid.Row="0" Grid.Column="2" VerticalAlignment="Center" Margin="0,5,10,5"/>
                            <TextBox x:Name="TxtContactCompany" Grid.Row="0" Grid.Column="3" Margin="0,5,10,5" Style="{StaticResource LightModernTextBox}"/>

                            <TextBlock Text="Città:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,10,5"/>
                            <TextBox x:Name="TxtContactCity" Grid.Row="1" Grid.Column="1" Margin="0,5,10,5" Style="{StaticResource LightModernTextBox}"/>

                            <TextBlock Text="Interno:" Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" Margin="0,5,10,5"/>
                            <TextBox x:Name="TxtContactInternal" Grid.Row="1" Grid.Column="3" Margin="0,5,10,5" Style="{StaticResource LightModernTextBox}"/>

                            <Button x:Name="BtnSaveContact" Grid.Row="2" Grid.Column="3" Content="Salva Contatto" Width="120" 
                                    HorizontalAlignment="Right" Margin="0,10,0,0" Style="{StaticResource LightModernButton}" Click="BtnSaveContact_Click"/>
                        </Grid>
                    </GroupBox>

                    <!-- Chiamate del contatto -->
                    <GroupBox Header="Chiamate del Contatto" Grid.Row="2" Style="{StaticResource LightModernGroupBox}">
                        <DataGrid x:Name="DgContactCalls" Style="{StaticResource LightModernDataGrid}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="ID" Binding="{Binding ID}" Width="60"/>
                                <DataGridTextColumn Header="Tipo" Binding="{Binding TipoChiamata}" Width="100"/>
                                <DataGridTextColumn Header="Data Arrivo" Binding="{Binding DataArrivoChiamata, StringFormat=dd/MM/yyyy HH:mm:ss}" Width="180"/>
                                <DataGridTextColumn Header="Data Fine" Binding="{Binding DataFineChiamata, StringFormat=dd/MM/yyyy HH:mm:ss}" Width="180"/>
                                <DataGridTextColumn Header="Durata (sec)" Binding="{Binding Durata.TotalSeconds, StringFormat=N0}" Width="120"/>
                                <DataGridTextColumn Header="Località" Binding="{Binding Extra}" Width="*"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </GroupBox>
                </Grid>
            </TabItem>

            <!-- Statistiche Tab -->
            <TabItem Header="STATISTICHE" Style="{StaticResource LightModernTabItem}">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <!-- Filtri -->
                        <RowDefinition Height="*"/>
                        <!-- Prima riga: Grafico a barre -->
                        <RowDefinition Height="*"/>
                        <!-- Seconda riga: Grafico a torta + Grafico a barre -->
                    </Grid.RowDefinitions>

                    <!-- Filtri statistiche -->
                    <GroupBox Header="Filtri Statistiche" Grid.Row="0" Style="{StaticResource LightModernGroupBox}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Periodo:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <ComboBox x:Name="CbTimeRange" Width="150" Margin="0,0,10,0">
                                <ComboBoxItem Content="Ultimi 7 giorni" IsSelected="True"/>
                                <ComboBoxItem Content="Ultimo mese"/>
                                <ComboBoxItem Content="Ultimo trimestre"/>
                                <ComboBoxItem Content="Ultimo anno"/>
                            </ComboBox>

                            <Button x:Name="BtnRefreshStats" Content="Aggiorna" Width="100" Margin="10,0,0,0" 
                        Style="{StaticResource LightModernButton}" Click="BtnRefreshStats_Click"/>
                        </StackPanel>
                    </GroupBox>

                    <!-- PRIMA RIGA: Solo grafico a barre -->
                    <Border Grid.Row="1" Style="{StaticResource ChartBorderStyle}" Margin="0,10,0,5">
                        <lvc:CartesianChart Series="{Binding SeriesCollection}" LegendLocation="Bottom" Hoverable="True">
                            <lvc:CartesianChart.AxisX>
                                <lvc:Axis Title="Tipo chiamata" Labels="{Binding Labels}" LabelsRotation="20" FontSize="9" FontStyle="Oblique" FontFamily="Bold">
                                    <lvc:Axis.Separator>
                                        <lvc:Separator Step="1"></lvc:Separator>
                                    </lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisX>
                            <lvc:CartesianChart.AxisY>
                                <lvc:Axis Title="Durata media chiamate">
                                    <lvc:Axis.Separator>
                                        <lvc:Separator></lvc:Separator>
                                    </lvc:Axis.Separator>
                                </lvc:Axis>
                            </lvc:CartesianChart.AxisY>
                        </lvc:CartesianChart>
                    </Border>

                    <!-- SECONDA RIGA: Grafico a torta + Grafico a barre affiancati -->
                    <Grid Grid.Row="2" Margin="0,5,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.2*"/>
                            <!-- Grafico a torta (20% dello spazio) -->
                            <ColumnDefinition Width="0.8*"/>
                            <!-- Grafico a barre (80% dello spazio) -->
                        </Grid.ColumnDefinitions>

                        <!-- Grafico a torta (più piccolo) -->
                        <Border Grid.Column="0" Style="{StaticResource ChartBorderStyle}" Margin="0,0,5,0">
                            <lvc:PieChart Series="{Binding PieSeries}" LegendLocation="Bottom" InnerRadius="70" Margin="10">
                                <lvc:PieChart.ChartLegend>
                                    <lvc:DefaultLegend BulletSize="15"></lvc:DefaultLegend>
                                </lvc:PieChart.ChartLegend>
                            </lvc:PieChart>
                        </Border>

                        <!-- Grafico a barre (più grande) -->
                        <Border Grid.Column="1" Style="{StaticResource ChartBorderStyle}" Margin="5,0,0,0">
                            <lvc:CartesianChart Series="{Binding SeriesCollection2}" LegendLocation="Bottom" Hoverable="True">
                                <lvc:CartesianChart.AxisX>
                                    <lvc:Axis Title="Tipo chiamata" Labels="{Binding Labels2}" LabelsRotation="20" FontSize="9" FontStyle="Oblique" FontFamily="Bold">
                                        <lvc:Axis.Separator>
                                            <lvc:Separator Step="1"></lvc:Separator>
                                        </lvc:Axis.Separator>
                                    </lvc:Axis>
                                </lvc:CartesianChart.AxisX>
                                <lvc:CartesianChart.AxisY>
                                    <lvc:Axis Title="Durata media chiamate">
                                        <lvc:Axis.Separator>
                                            <lvc:Separator></lvc:Separator>
                                        </lvc:Axis.Separator>
                                    </lvc:Axis>
                                </lvc:CartesianChart.AxisY>
                            </lvc:CartesianChart>
                        </Border>
                    </Grid>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</Window>





